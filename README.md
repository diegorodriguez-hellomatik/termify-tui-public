# termify-tui-public

Public bootstrap repo for installing `termify` with:

```sh
curl -fsSL https://raw.githubusercontent.com/diegorodriguez-hellomatik/termify-tui-public/main/install.sh | sh
```

Current public assets:

- `termify-darwin-arm64.tar.gz`
- `termify-darwin-x64.tar.gz`
